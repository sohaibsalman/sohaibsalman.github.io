import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    padding: "50px",
  },
  subHeading: {
    margin: "10px 0",
    fontWeight: "lighter",
  },
  buttons: {
    margin: "15px 0",
  },
});

export { useStyles };

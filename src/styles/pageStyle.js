import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    padding: "50px",
  },
  paper: {
    padding: "15px 30px",
  },
  timeline: {
    "*": {
      flex: 0,
    },
  },
});

export { useStyles };
